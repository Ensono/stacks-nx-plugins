{
    "$schema": "http://json-schema.org/schema",
    "cli": "nx",
    "$id": "NextAuthRedis",
    "title": "Ensono Stacks Next Auth Redis Deployment Generator",
    "description": "The @ensono-stacks/next:next-auth-redis-deployment generator will add relevant infra for Redis in an existing Next-Auth app.",
    "type": "object",
    "properties": {
        "project": {
            "type": "string",
            "description": "The name of the project.",
            "alias": "p",
            "$default": {
                "$source": "projectName"
            },
            "x-prompt": "What is the name of the project?",
            "x-priority": "important"
        }
    },
    "required": ["project"]
}
