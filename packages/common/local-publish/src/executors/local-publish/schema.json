{
    "$schema": "http://json-schema.org/schema",
    "version": 2,
    "title": "Local Publish Executor",
    "description": "",
    "type": "object",
    "properties": {
        "buildCommand": {
            "type": "string",
            "description": "The command to run to build all packages"
        }
    },
    "required": ["buildCommand"]
}
